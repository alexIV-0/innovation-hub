-- Хронометраж ИСХОДНИКА в архиве обработок.
--
-- Нужен паре осей «вход × секунда» (docs/BILLING_AND_TRIAL_PLAN.md, П6): она
-- закрывает случай, на котором ломается счёт по выходу — подготовка субтитров
-- даёт диалог на три минуты и пять дорожек, и `out_sec` приезжает как 900.
-- Считая вход, мы получаем три минуты независимо от того, сколько дорожек
-- родилось в процессе.
--
-- Побочно это ещё и точный резерв: количество известно ДО обработки, значит ни
-- перерасхода, ни списаний в убыток в таких проектах не возникает.
--
-- Поле схемы v2 архива: пишет его машина отдельным ffprobe-проходом по
-- источнику (fs.manager.tauri/ideasAndTest/BILLING_NODE_CONTRACT_PLAN.md §2).
-- Колонка заводится заранее и остаётся NULL, пока программа не начнёт слать:
-- приёмник читает обе версии схемы, старые строки остаются валидными.
ALTER TABLE processing_stats ADD COLUMN IF NOT EXISTS src_sec INTEGER;
